package com.example.classexamples;

public class OuterClass {

	public static class InnerClass{
		// we have created a nested class AND it happens to be static.
		// inner class is only used by the outer class
			public static class VeryInnerClass{
				private class TheInnerestClass{
					
				}
			}
	}
}
